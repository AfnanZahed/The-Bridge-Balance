---
id: 0054
title: Execute intro resequence Phase 1 (five-chapter structure)
stage: general
date: 2026-09-10
surface: agent
model: claude-sonnet-5
feature: none
branch: none (no version control in this project)
user: afnanzahed@gmail.com
command: none (freeform chat message — execution brief)
labels: ["intro-sequence", "curriculum-architecture", "chapter-production", "lesson-spine-authoring", "ledgers", "phase-1-execution"]
links:
  spec: null
  ticket: null
  adr: null
  pr: null
files:
  - edu-site/docs/intro-1-binary-to-programming.md (created)
  - edu-site/docs/intro-2-architecture-map.md (created)
  - edu-site/docs/intro-3-editors-and-ides.md (created)
  - edu-site/docs/intro-4-terminals-and-cli-agents.md (created)
  - edu-site/docs/intro-5-spec-driven-engineering.md (created, raw merge)
  - edu-site/docs/intro.md (deleted)
  - edu-site/docs/intro-2-binary-to-language.md (deleted)
  - edu-site/docs/intro-3-architecture-map.md (deleted)
  - edu-site/docs/intro-4-two-extremes-and-sde.md (deleted)
  - edu-site/sidebars.ts (Start Here category rewired to the five new ids)
  - curriculum-state/ledgers/prerequisite-graph.yaml (four intro rows replaced by five; progress block updated)
  - curriculum-state/ledgers/concept-ledger.yaml (re-pointed to intro-5; dated header note)
  - curriculum-state/ledgers/evidence-ledger.yaml (all questions_asked re-pointed to intro-5; dated header note)
  - curriculum-state/ledgers/example-ledger.yaml (both entries re-pointed to intro-5; dated header note)
  - curriculum-state/contracts/calibration.md (anchor section rewritten — anchor now unset)
  - curriculum-state/README.md (provenance updated)
  - curriculum-state/canon/thesis.md (source-list reference re-pointed to intro-5)
  - CLAUDE.md (current-state line corrected)
  - INSTALL.md (example command + Current state section corrected)
  - PROJECT-MAP.md (inventory line corrected)
  - .claude/skills/bridge-balance-project-guide/reference/project-map.md (inventory line corrected)
  - .claude/skills/bridge-balance-project-guide/reference/chapter-production/reference/research.md (worked-example path corrected)
  - edu-site/README.md (docs tree updated)
  - edu-site/docs/glossary.md (five /intro links re-pointed to /intro-5-spec-driven-engineering)
  - edu-site/scripts/check-frontmatter.mjs (removed stale intro.md from STAGE_FILES exemption)
  - edu-site/scripts/check-references.mjs (proposals indexed but no longer scanned as a reference source)
  - edu-site/src/data/chapterManifest.ts (regenerated via npm run gen:manifest — never hand-edited)
  - edu-site/static/search-index.json (regenerated by the build)
  - curriculum-state/proposals/stage-00-intro-resequence-2026-09-10.md (Phase 1 executed note appended)
tests:
  - npm run check:frontmatter -> passed
  - npm run check:refs -> passed (after excluding curriculum-state/proposals from the scanned set)
  - npm run check:chapter -> 0 checked, 26 skipped (all five intro files listed as placeholder), 0 errors, 0 warnings
  - npm run gen:manifest -> 20 states
  - npm run build -> all five gates pass, Docusaurus "Generated static files in build"
---

## Prompt

# Execute: resequence the intro chapters (5-chapter evolutionary structure) and author them

You are working in **The Bridge Balance** book repository (`Book/`), a free
Spec-Driven Engineering curriculum built with Docusaurus. This repo has no
git — never run version-control commands. You are picking up a fully
decided, user-approved restructuring of the book's opening "Start Here"
chapter sequence. Nothing below is speculative: every decision was already
made by the project owner across a multi-turn planning conversation. Your
job is to execute it, in two phases, and Phase 2 requires you to actually
**master a specific authoring skill before writing a single sentence of
chapter prose.**

## Read these files, fully, before doing anything else

In this order:

1. `CLAUDE.md` (repo root) — the whole operating contract for this repo:
   PHR-on-every-prompt requirement, ADR-suggestion protocol, the content
   track vs. platform track split, and the standing rules that override
   default behavior (no version control, no fixed chapter count ever, no
   images/diagrams/Mermaid generated by you — every image is the project
   owner's, and text-only chapters ship without one where needed).
2. `curriculum-state/README.md` — the book's shared-memory system. Read
   this before touching any ledger.
3. All four files in `curriculum-state/canon/` (`thesis.md`, `audience.md`,
   `voice.md`, `integrity-floor.md`) — small, and they change the shape of
   everything you write.
4. `curriculum-state/contracts/calibration.md` — the reading-time/scope
   rules. Note: this file currently names `edu-site/docs/intro.md` as the
   calibration anchor at `scope_multiplier: 1.0` — that file is being
   retired as part of this task (see Phase 1 below), so this file needs an
   honest update, not just a silent mismatch.
5. `curriculum-state/proposals/stage-00-intro-resequence-2026-09-10.md` —
   **the actual design document for this task.** It went through three
   revision passes with the project owner and contains: the full 5-chapter
   structure, the reasoning for where "What Is Programming" belongs, the
   filename mapping, and three resolved risk items (finale length →
   sub-chapter contingency; tool-comparison dating → a Callout notice;
   intro-1 scope growth → a non-issue). Read it in full. It is the source
   of truth for *what* the new structure is; this prompt tells you *how* to
   execute it.
6. The three official planning documents — genuine shared source material
   for this book despite living in a sibling, otherwise out-of-scope
   folder (do not read or touch anything else in that sibling folder):
   - `The Bridge Balance/Official docs/curriculum_1.md`
   - `The Bridge Balance/Official docs/problem_statement.md`
   - `The Bridge Balance/Official docs/solution_statement.md`
   `solution_statement.md` draws a deliberate line between **Spec-Driven
   Development (SDD)** — the existing, tool-adopted methodology — and
   **Spec-Driven Engineering (SDE)** — this book's own wider discipline,
   named explicitly as SDD's successor. Neither current `intro.md` nor
   `intro-4-two-extremes-and-sde.md` states this distinction by name.
   Decide deliberately, when you write the finale chapter (Phase 2, chapter
   5), whether to draw it explicitly — the official solution statement
   clearly wants it drawn somewhere in the curriculum.
7. **`.claude/skills/lesson-spine-authoring/` — read `SKILL.md` and every
   reference file under this skill's directory, completely, in full.** This
   is not optional background reading — it is the actual method you will
   use to write every sentence of every chapter in Phase 2. It is a
   twelve-station "Name-First" spine (Prime–Build–Close: Name, Anchor,
   Source, Why, What, How, Where, Cost, When, Lineage, Retrieve, Spiral),
   built on 5W2H plus etymological decomposition, for a deliberately mixed
   audience — a senior engineer and someone who struggled to finish a
   signup form last week, both reading the same document, neither served
   by a separate edition. It also governs language register (plain English,
   15–18 word average sentence length, technical depth left untouched) and
   figure specs. Two pilot chapters already proved this approach out
   successfully earlier in this project (they were later deleted once the
   pilot succeeded, ahead of applying the same treatment for real — which
   is what you are now doing). Do not attempt to write chapter prose from a
   summary or from memory of "spine-based teaching" in general — read the
   actual skill files, because the station order, the two-reader rule, and
   the register rules are specific and load-bearing.
8. `edu-site/sidebars.ts` — current sidebar wiring you will edit.
9. All four files in `curriculum-state/ledgers/`: `prerequisite-graph.yaml`,
   `concept-ledger.yaml`, `evidence-ledger.yaml`, `example-ledger.yaml`.
10. The four current chapter files, in full — you are retiring/migrating
    all of them: `edu-site/docs/intro.md`,
    `edu-site/docs/intro-2-binary-to-language.md`,
    `edu-site/docs/intro-3-architecture-map.md`,
    `edu-site/docs/intro-4-two-extremes-and-sde.md`.
11. `edu-site/src/components/Callout/index.tsx` — the only admonition
    component the build allows in a chapter body (alongside `StageBanner`).
    Its API: `<Callout type="info" title="...">children</Callout>`, with
    `type` one of `info | tip | important | warning | danger`. You will use
    this for the "tools current as of writing" notices in chapters 3 and 4.

## Background: why this restructuring is happening

The intro sequence currently opens with Spec-Driven Engineering (SDE) in
chapter 1 (`intro.md`), then spends chapters 2–4 on binary/language,
architecture, and (again) the two extremes and SDE. The project owner
flagged this as backwards: SDE is the *destination* of a beginner's journey,
not its opening move. The whole sequence is being reorganized around one
organizing principle, in the owner's own words: **an evolutionary thought
process — "0/1 -> .... -> SDE."** Every chapter is a rung on a ladder that
starts at raw binary and ends at the book's own engineering discipline,
built because the rung before it ran out of room — nothing is taught before
its natural place in that timeline.

Concretely this produced a 5-chapter structure (full detail and reasoning in
the proposal file from step 5 above):

1. **`intro-1-binary-to-programming.md`** — replaces
   `intro-2-binary-to-language.md`. Same transistor → bit → byte → ASCII →
   machine code → x86/ARM → punched cards → assembly → assembler → register
   chain, with one new beat, **"What Is Programming,"** inserted
   immediately after "register" and immediately before "high-level
   language" — the actual historical pivot from talking to the machine in
   its own terms to talking in a language built for humans. This is NOT a
   concept-first preface before the binary content (an earlier draft of
   this plan tried that ordering and the owner explicitly corrected it) —
   it is a rung positioned at its real evolutionary place. Then continues:
   high-level language → compiler → interpreter → abstraction.
2. **`intro-2-architecture-map.md`** — replaces
   `intro-3-architecture-map.md`. Content and scope unchanged — the "let's
   build one thing together" full-stack tour (platform, frontend, backend,
   API/HTTP/REST, database, auth, Git/GitHub, deployment, the same-map
   cross-platform callback, cybersecurity as a cross-cutting lens).
3. **`intro-3-editors-and-ides.md`** — new. From text editors to IDEs (how
   dev environments evolved) → What Is an IDE? (VS Code — owner will supply
   a real screenshot or short official clip) → AI-integrated IDEs (Cursor,
   Windsurf, Zed, Trae & more — same treatment as VS Code) → AI-assisted vs.
   AI-driven development, key differences.
4. **`intro-4-terminals-and-cli-agents.md`** — new. What is a terminal? →
   terminal types (CMD, PowerShell, Bash, Git Bash) → why terminal skills
   are non-negotiable → CLI-based coding agents, the next level (Claude
   Code, Codex, Gemini CLI — same treatment as VS Code) → how CLI agents
   use the terminal to code → Claude Code/Codex/other CLI agents, which is
   most famous → who should use Cursor vs. Claude Code.
5. **`intro-5-spec-driven-engineering.md`** — new finale. Full merge of
   `intro.md` (all of it — evidence studies, worked examples, the
   Specify/Design/Implement/Verify loop, Development-vs-Engineering) and
   `intro-4-two-extremes-and-sde.md` (vibe coding, over-caution, the two
   extremes, the safety floor), checked against the official docs from step
   6 above. This is the destination: everything the reader has now met —
   programming, architecture, IDEs, terminals, agents — gets named as
   exactly what SDE governs.

Three risks were flagged and resolved by the owner (full text in the
proposal file):
- **If `intro-5` turns out disproportionately long** once drafted: do not
  ship it as one flat file. Restructure it as its own sidebar **category**
  with nested sub-lesson docs — the same pattern Stage 1–4 already use (a
  folder holding an index doc plus numbered sub-lessons, wired into
  `sidebars.ts` as a collapsible category, not a single leaf item).
- **Chapters 3 and 4 name specific competing tools** (Cursor, Windsurf,
  Zed, Trae, Claude Code, Codex, Gemini CLI) that will date quickly. Both
  chapters need an explicit `<Callout type="info" title="Current as of
  writing">` (or `type="important"` if it should read more urgently) near
  where the named tools are introduced, stating the actual date you write
  the chapter and telling the reader to check the current landscape.
- **`intro-1`'s scope growing** was judged a non-issue — the concept of a
  programming *language* already existed implicitly in the old
  `intro-2-binary-to-language.md` (it already ends at "high-level
  language"); "What Is Programming" just makes that explicit for a
  beginner reader.

One more thing to reconcile while merging chapter 5: `intro.md`'s own
`teaches` list (see `prerequisite-graph.yaml`) names both "the
specify-delegate-inspect-verify framework" and "the specify-design-
implement-verify responsibility loop" — check whether these are the same
four-part framework under two different names or genuinely distinct, and
make the finale consistent. Similarly, `intro.md` says "the two failure
poles (surrender and resistance)" while `intro-4` says "the two extremes
(over-caution and over-trust)" — these read like the same pair under
different names (surrender ≈ over-trust, resistance ≈ over-caution) and
should land on one consistent naming in the merged chapter.

## Phase 1 — mechanical restructuring (do this fully before Phase 2)

Goal: get the new 5-chapter scaffolding correctly wired — sidebar, all four
ledgers, the manifest — with placeholder content in place, and the build
green, before writing any new prose.

1. Create the 5 new files at their new paths:
   - `docs/intro-1-binary-to-programming.md` — migrate
     `intro-2-binary-to-language.md`'s content as-is (do not add "What Is
     Programming" yet — that is real authoring, Phase 2 work). Update
     frontmatter (`title`, `sidebar_label`, `sidebar_position: 0`) to fit
     its new position as chapter 1. Keep `chapter_state: "placeholder"`.
   - `docs/intro-2-architecture-map.md` — migrate
     `intro-3-architecture-map.md`'s content as-is, `sidebar_position: 1`,
     `chapter_state: "placeholder"`.
   - `docs/intro-3-editors-and-ides.md` — new stub. Use an existing
     placeholder's frontmatter shape as a template (see
     `intro-2-binary-to-language.md`'s frontmatter: `sidebar_label`,
     `sidebar_position`, `title`, `description`, `keywords`,
     `chapter_state`, `video_url`). Body can be the topic list from the
     proposal as a skeleton — real prose is Phase 2. `sidebar_position: 2`,
     `chapter_state: "placeholder"`.
   - `docs/intro-4-terminals-and-cli-agents.md` — same treatment,
     `sidebar_position: 3`, `chapter_state: "placeholder"`.
   - `docs/intro-5-spec-driven-engineering.md` — concatenate `intro.md`'s
     full content followed by `intro-4-two-extremes-and-sde.md`'s full
     content. Mark clearly (an HTML comment near the top is fine) that this
     is a raw, unedited merge pending the real Phase 2 spine-authoring
     pass — do not present it as finished prose. `sidebar_position: 4`.
     Set `chapter_state: "placeholder"` (do **not** carry over `intro.md`'s
     `text-ready` status — the merged file hasn't been verified as a
     coherent chapter yet). Set `scope_multiplier: null` (do not blindly
     carry forward `1.0` — real scope gets assigned when this chapter is
     actually authored in Phase 2, per `contracts/calibration.md`'s own
     rule that a multiplier needs a reasoned `scope_reason`, not a
     carried-over guess).
2. Delete the four old files once their content is migrated: `intro.md`,
   `intro-2-binary-to-language.md`, `intro-3-architecture-map.md`,
   `intro-4-two-extremes-and-sde.md`.
3. Edit `edu-site/sidebars.ts`: the "Start Here" category's `link` currently
   points at `{ type: "doc", id: "intro" }` — change it to
   `intro-1-binary-to-programming`. Its `items` array currently lists the
   three old placeholders — replace with all 5 new ids in order:
   `intro-1-binary-to-programming`, `intro-2-architecture-map`,
   `intro-3-editors-and-ides`, `intro-4-terminals-and-cli-agents`,
   `intro-5-spec-driven-engineering`.
4. Edit `curriculum-state/ledgers/prerequisite-graph.yaml`: replace the four
   intro-sequence rows (`intro`, `intro-2`, `intro-3`, `intro-4`) with five
   new rows (`intro-1` through `intro-5`) — new `path`, `title`,
   `position: 0..4`, `status: placeholder` for all five, a straight
   `requires` chain (each requires the previous), and `teaches` lists
   carrying over the old content plus the new topics (add "What Is
   Programming" to `intro-1`; write real `teaches` lists for `intro-3` and
   `intro-4` from the topic list in the proposal; merge and de-duplicate
   `intro` and `intro-4`'s old `teaches` lists into `intro-5`, resolving
   the two naming inconsistencies flagged above). Write a `notes` field on
   each explaining the resequencing and pointing back at
   `curriculum-state/proposals/stage-00-intro-resequence-2026-09-10.md`.
   Update the `progress:` block's `total_lesson_files` count and its `note`
   field to describe this change, following the existing pattern of prior
   dated notes in that file (do not silently overwrite the history of
   prior notes — append).
5. Edit `curriculum-state/ledgers/concept-ledger.yaml`: every entry
   currently has `first_defined` and/or `used_in` pointing at
   `docs/intro.md` (verified — no entry in this file references
   `intro-2`/`intro-3`/`intro-4` at all). Re-point every one of those to
   `docs/intro-5-spec-driven-engineering.md`. Update the `seeded_from` note
   at the top of the file if it still says `docs/intro.md`.
6. Edit `curriculum-state/ledgers/evidence-ledger.yaml`: every single
   `questions_asked` row, across every source (~20 sources — verified, none
   reference `intro-2`/`intro-3`/`intro-4`), has `chapter: docs/intro.md`.
   Re-point every one to `docs/intro-5-spec-driven-engineering.md`.
7. Edit `curriculum-state/ledgers/example-ledger.yaml`: the
   `intro-no-single-example` and `intro-worked-examples` entries both have
   `chapter: docs/intro.md` — re-point both to
   `docs/intro-5-spec-driven-engineering.md`.
8. Edit `curriculum-state/contracts/calibration.md`: the "anchor at scope
   1.0" section names `edu-site/docs/intro.md` explicitly by path. That
   file no longer exists after step 2. Rewrite this section honestly: the
   anchor is unset until `intro-5-spec-driven-engineering.md` is actually
   re-authored (Phase 2) and reaches `text-ready` again — at that point,
   remeasure the anchor from the gate's own reported word count, per this
   file's existing instruction ("re-read it from the gate output rather
   than from this file, which will drift"). Do not leave this file pointing
   at a deleted path.
9. From `edu-site/`, run `npm run gen:manifest` to regenerate
   `src/data/chapterManifest.ts`. Never hand-edit that file — it is
   generated and says so at its own top.
10. From `edu-site/`, run the verification gates — `npm run build` chains
   all five (`check:frontmatter`, `check:refs`, `check:chapter`, manifest
   generation, search-index generation, then the Docusaurus build) in
   order. Fix anything that fails before moving to Phase 2. A grep across
   the repo (excluding `.docusaurus/`, `build/`, `history/`) for the four
   old filenames should turn up nothing outside historical PHR/proposal
   text.
11. Append a short, dated note to the end of
    `curriculum-state/proposals/stage-00-intro-resequence-2026-09-10.md`
    recording that Phase 1 executed, with today's date — follow the same
    "Revised <date>" pattern already used twice in that file.
12. File a PHR for this work under `history/prompts/general/` per
    `CLAUDE.md`'s process (check the highest existing ID in that directory
    first and increment from there).

## Phase 2 — actually author the five chapters

Do this **one chapter at a time, in order (1 → 5)**, not all at once — later
chapters build on earlier ones conceptually, and the project owner reviews
each chapter on localhost before it's considered settled. For each chapter:

1. Describe the task in plain language matching this repo's own routing
   convention (`CLAUDE.md`): something like "make `intro-1` text-ready" or
   "write the lesson on text editors to AI-integrated IDEs for intro-3."
   This is what triggers `chapter-production`, which owns research/scope/
   assembly/the gate/ledger write-back, and which hands the actual drafting
   to `lesson-spine-authoring` at its step 3 — the skill you read in full
   above. Do not skip straight to writing prose yourself without going
   through that structure.
2. For **chapter 1** (`intro-1-binary-to-programming.md`): insert "What Is
   Programming" at its agreed position (after register, before high-level
   language) and run the whole chapter through the real spine treatment —
   do not just lightly edit the migrated placeholder prose.
3. For **chapter 2** (`intro-2-architecture-map.md`): spine-treat the
   existing content. No new topics.
4. For **chapter 3** (`intro-3-editors-and-ides.md`): fresh research and
   drafting from its topic list above. Include the "current as of writing"
   Callout.
5. For **chapter 4** (`intro-4-terminals-and-cli-agents.md`): fresh
   research and drafting from its topic list above. Include the same kind
   of Callout.
6. For **chapter 5** (`intro-5-spec-driven-engineering.md`): this is a real
   merge-and-rewrite, not a light edit of the concatenated placeholder —
   decide on the SDD-vs-SDE naming question, reconcile the two naming
   inconsistencies flagged above, re-verify every inherited evidence
   citation still reads correctly in its new position, and **check the
   drafted length against the other four chapters.** If it's
   disproportionately long, restructure it as its own sidebar category with
   nested sub-lesson docs (the Stage 1–4 folder pattern) instead of
   shipping one giant flat file — this was pre-agreed by the project owner,
   not something to ask permission for again.
7. After each chapter is drafted: run
   `node scripts/check-chapter.mjs --chapter docs/<path>` from `edu-site/`,
   fix any gate failures, confirm the four ledgers were written back to
   (this is `chapter-production`'s own responsibility, but verify it
   happened), and only then flip that chapter's `chapter_state` to
   `"text-ready"`. Do not flip it before the gate passes.
8. File a PHR for each chapter's authoring pass.

## Non-negotiable constraints (carry these the whole time)

- No version control commands, ever — this repo has none, by design.
- Never state or imply a fixed chapter count or a finalized table of
  contents anywhere (`CLAUDE.md` locked decision D5) — an earlier draft of
  this project did exactly that and had to be corrected in four files.
- You do not generate images, diagrams, SVG, or Mermaid — every chapter
  image is the project owner's own. A chapter that would benefit from a
  diagram ships without one and names the gap, unless explicitly asked for
  an image *prompt* (text only).
- Only `<Callout>` and `<StageBanner>` are registered chapter-body
  components — anything else fails the build.
- Every technical term is glossed inline at first use, in every chapter,
  every time — a reader arriving from a search result has no earlier
  chapter. No audience-labeling phrases ("if you're new to this,"
  "experienced readers will know").
- Examples are real and named — no `foo`, no Acme Corp, no to-do app.
- Create a PHR after every user prompt you receive while doing this work,
  and after each major milestone, per `CLAUDE.md`'s process.
- If you detect an architecturally significant decision along the way,
  surface the ADR suggestion text from `CLAUDE.md` — never auto-create one.

## Definition of done

- [ ] Five new chapter files exist at their new paths; the four old files
      are gone.
- [ ] `sidebars.ts` wired to the five new ids, in evolutionary order.
- [ ] All four ledgers plus `contracts/calibration.md` re-pointed — a
      repo-wide grep for the four old filenames (excluding build artifacts
      and `history/`) turns up nothing live.
- [ ] `chapterManifest.ts` regenerated via the script, never hand-edited.
- [ ] All five build gates pass.
- [ ] Every chapter has actually been through `lesson-spine-authoring`
      (not just migrated/concatenated prose) before being marked
      `text-ready`.
- [ ] Chapters 3 and 4 carry the "current as of writing" Callout.
- [ ] Chapter 5's length was checked and split into a sidebar sub-category
      if it came out disproportionately long.
- [ ] A PHR exists for the restructuring and for each chapter's authoring
      pass.

## Response snapshot

Read the full mandated reading list first — `CLAUDE.md`, `curriculum-state/README.md`,
all four canon files, `contracts/calibration.md`, the design proposal, all four ledgers,
all four current chapters, `sidebars.ts`, the `Callout` component, plus the three
official docs (from the sibling `The Bridge Balance/Official docs/` folder) and the
entire `.claude/skills/lesson-spine-authoring/` tree (SKILL.md and all four references),
and the bundled `chapter-production` protocol and its four references. Only then touched
a file.

Executed **Phase 1** end to end:

1. **Created the five chapters.** `intro-1` migrates the old
   `intro-2-binary-to-language.md` body verbatim with re-positioned frontmatter
   (`sidebar_position: 0`) and a title matching the new position; `intro-2` migrates the
   old architecture map unchanged at `sidebar_position: 1`; `intro-3` and `intro-4` are
   new placeholder stubs carrying the proposal's topic lists; `intro-5` is a single
   frontmatter block followed by the old `intro.md` body and then the old `intro-4` body,
   under a prominent HTML comment declaring it a raw, unauthored merge with the five
   outstanding Phase 2 items listed. All five are `chapter_state: "placeholder"`;
   `intro-5` is `scope_multiplier: null`.
2. **Deleted** the four retired files.
3. **Sidebar** rewired: the "Start Here" category now links
   `intro-1-binary-to-programming` and lists all five ids in order.
4. **prerequisite-graph.yaml**: four intro rows replaced by five, straight `requires`
   chain, per-row notes pointing at the proposal, `teaches` lists built out for the new
   topics, and the two naming inconsistencies resolved on `intro-5`'s row (the
   specify-delegate-inspect-verify framework == the specify-design-implement-verify loop;
   the two failure poles == the two extremes). The progress block's count and note were
   updated by appending, not overwriting; `authored` dropped from 1 to 0 because
   `intro.md` no longer exists as a text-ready file.
5. **All three other ledgers** re-pointed (`concept`: 14 replacements, `evidence`: 48,
   `example`: 3), each with a dated header note explaining the move.
6. **calibration.md** anchor section rewritten: anchor now unset until `intro-5` is
   re-authored and passes the gate; the stale scope-table reference to "the intro" removed.
7. **Build clean.** Four unforeseen breaks surfaced and were fixed, all documented in the
   appended proposal note: `check-frontmatter.mjs` hard-coded `intro.md` in a
   must-exist exemption list; `check-references.mjs` scanned the proposals folder, so the
   planning documents (including the verbatim saved prompt) failed the gate once the files
   they name were retired — proposals are now indexed but not scanned as sources, matching
   the existing `history/`/`specs/` treatment; `glossary.md` linked five definitions to
   the retired `/intro` route; and several live docs (`CLAUDE.md`, `INSTALL.md`,
   `PROJECT-MAP.md`, the project map skill reference, `research.md`, `edu-site/README.md`,
   `curriculum-state/README.md`, `canon/thesis.md`) carried now-false claims about
   `intro.md` being the only text-ready chapter. `npm run build` now passes all five gates
   and generates static files. `chapterManifest.ts` regenerated (20 states) via the
   script.

Phase 2 was not started, deliberately: the brief requires each chapter to go through the
full `chapter-production` → `lesson-spine-authoring` pipeline one at a time, with owner
review on localhost between them.

## Outcome

- ✅ Impact: The approved five-chapter evolutionary intro structure is now live and
  fully wired — files, sidebar, all four ledgers, calibration anchor, manifest — with the
  build green, ready for Phase 2 authoring. Six latent defects in the repo's own
  validators/docs were found and fixed along the way (a hard-coded exemption that would
  have broken the build, a reference-scanning scope error, a dead glossary route, and
  four stale "intro.md is text-ready" claims).
- 🧪 Tests: `npm run check:frontmatter` passed; `npm run check:refs` passed;
  `npm run check:chapter` → 0 checked, 26 skipped (all five intro files correctly listed
  by name as placeholder), 0 errors, 0 warnings; `npm run gen:manifest` → 20 states;
  `npm run build` → all five gates pass and Docusaurus reports "Generated static files".
- 📁 Files: 5 chapters created, 4 deleted; `sidebars.ts`; all 4 ledgers;
  `contracts/calibration.md`; `curriculum-state/README.md`; `canon/thesis.md`;
  `CLAUDE.md`; `INSTALL.md`; `PROJECT-MAP.md`; the project-guide `project-map.md` and
  `research.md`; `edu-site/README.md`; `edu-site/docs/glossary.md`;
  `check-frontmatter.mjs`; `check-references.mjs`; regenerated `chapterManifest.ts` and
  `search-index.json`; the proposal file.
- 🔁 Next prompts: Phase 2, chapter 1 first — "make `intro-1` text-ready", which routes
  through `chapter-production` and `lesson-spine-authoring`. Chapters 3 and 4 can only be
  authored with real current tool research; chapter 5 needs the length judgment.
- 🧠 Reflection: The two validator changes were the only judgment calls the brief did not
  cover. Both were resolved toward the gate's own stated intent rather than around it:
  `check-frontmatter`'s exemption list asserts its entries exist, so removing the deleted
  file was required, not optional; and `check-references` already excludes `history/` and
  `specs/` as "historical records allowed to name removed files", so extending that same
  reasoning to non-binding proposals — while still *indexing* them so live docs can point
  at them — is consistency, not a loosened check.

## Evaluation notes (flywheel)

- Failure modes observed: none in Phase 1 after fixes; the four unexpected build breaks
  were pre-existing latent defects surfaced by the file retirement, not introduced by the
  restructuring.
- Graders run and results (PASS/FAIL): frontmatter PASS; references PASS; chapter gate
  PASS (all placeholders skipped by design, listed by name); manifest PASS (20 states);
  Docusaurus build PASS.
- Prompt variant (if applicable): n/a
- Next experiment (smallest change to try): Phase 2 chapter 1, through the full
  `chapter-production` → `lesson-spine-authoring` path, then check whether the gate's
  scope-reason enforcement and the retrieval-prompt ending both survive a real chapter
  with no owner-supplied image.
