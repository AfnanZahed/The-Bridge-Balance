# Progress — Feature 012: Chatbot Tutor (The Bridge Balance Tutor 1.0)

One dated line per step, oldest first. Every line is written only after the step's evidence
was read from real output (build prompt §0.5). Session model: DeepSeek V4.1 Flash (Command
Code).

Resume rule (build prompt §0.3): read `_scratch/platform-research/02-chatbot-1.0-build-prompt.md`,
then this file and `tasks.md`, and resume at the first unticked task.

| Date | Step | What happened | Evidence |
|---|---|---|---|
| 2026-09-23 | 0.0 | Feature folder created by hand at `specs/012-chatbot-tutor/` with `progress.md` and `research.md`. | `read_directory specs/` shows 011 as the previous highest; this folder now exists. The stock `create-new-feature.ps1` was **not** run: it calls `git checkout -b`, which the hard rules forbid ("`main` only. No branches"). Its own adapted command doc (`.claude/commands/sp.specify.md`) says to create the directory with file tools instead, so the folder was made by hand. |
| 2026-09-23 | 0.1 | Rule files updated: `CLAUDE.md` (3 bullets) and `bridge-balance-project-guide/SKILL.md` (2 bullets). | **CLAUDE.md** — the "No version control in the working process." bullet replaced with block A; the "Phase A only, today." bullet replaced with block B; the "Repo root DOES have `.git`." bullet rewritten to say git is now the working process and `ci.yml` runs on every push to `main` (its `pull_request` trigger stays, unused — both verified at `.github/workflows/ci.yml:4-7`: `pull_request: branches: [main, "00*-*"]`, `push: branches: [main]`). **SKILL.md** — Step 0 rule 9 and its "Phase A only, today" bullet carried the same meaning. Audit grep for "No version control", "no branches", "no commits", "Phase A only" over the six required locations: only remaining in-scope stale hit is `.specify/memory/constitution.md:546`, whose exact diff is prepared and shown at CP0 and **not** changed; `PROJECT-MAP.md`, `curriculum-state/canon/**` and `stack.md` carry no hit of the four phrases (`stack.md:23` separately calls the repo public — a CP2 item). Other hits are out-of-scope dated records (see CP0 list). |
| 2026-09-23 | 0.2 | Git baseline: state checked, `.gitignore` extended, secret scan run, commit `80ac6fc` made — **not pushed**. | `git remote -v` → `origin https://github.com/AfnanZahed/The-Bridge-Balance.git`; `git branch --show-current` → `main`. **`gh repo view --json visibility` → `{"isPrivate":false,"visibility":"PUBLIC"}`** — this contradicts block A and §2 row 16 ("the repo stays private"), so it is flagged at CP0 as a push blocker rather than guessed at. `.env.local` confirmed ignored (`.gitignore:49`); `.gitignore` gained `.env.*` above the `!.env.example` negation, plus `*.local`, `edu-site/api/.venv/`, `edu-site/api/.qdrant-local/`, `edu-site/.rag/`, `**/__pycache__/`, `edu-site/api/evals/reports/` (`.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`, `__pycache__/` were already present). `git check-ignore -v` verified `.env.example` is still **not** ignored while `.env.local`, `edu-site/api/.env`, `edu-site/api/.env.production`, `.qdrant-local/`, `.rag/`, `evals/reports/` and `.venv/` all are. Large-file scan: no file over 50 MB (excluding `node_modules`, `.git`, `.venv`). Secret check: no `.env`/`.env.*` staged — only the two `.env.example` files are tracked and both are unchanged; the staged-diff grep (`user_`, `sk-`, `ghp_`, `gho_`, `VERCEL_OIDC_TOKEN`, `PRIVATE KEY`, password-bearing `postgres://`) returned no credential value — every hit is a variable name (`INSTAGRAM_POST_IG_USER_MEDIA`), a CSS class (`task-list-item`, `mask-image`), a placeholder (`sk-tracing-123`) or the build prompt's own instruction text; `git ls-files` confirms `.env.local` was never tracked. Commit `80ac6fc` = 366 files, 54,516 insertions; working tree **clean** afterwards, so `core.autocrlf=true` caused no spurious dirty state. |
| 2026-09-23 | 0.3 | Environment baseline: **frontend PASS, backend PASS** once the unsatisfiable `better-auth` dependency was removed. | Toolchain: Python 3.12.10, Node v24.18.0, npm 11.16.0. **Frontend** (delegated to a subagent; every result read from real output): `npm run typecheck` exit 0, no diagnostics; `npm run lint` exit 1 = **64 errors / 3 warnings / 9 infos** vs the 2026-09-22 baseline in `specs/011-curriculum-redesign/tasks.md` T020 (64/3/10) — identical but for one fewer info; `npm run build` exit 0, all five gates ran (`check:frontmatter` ✓, `check:refs` ✓, `check:chapter` 11 checked / 20 skipped / 0 errors / 11 warnings, then manifest and search index written) and Docusaurus reached `[SUCCESS] Generated static files in "build"`. One non-fatal pre-existing warning: broken anchor `/changelog → /faq#what-does-the-cs50-certification-cost`. The regenerated `chapterManifest.ts` and `search-index.json` came back **byte-identical**. **Backend:** `.venv` created; `pip install -e ".[dev]"` first failed on `better-auth>=0.4.0` (see the unblocking change below), then succeeded — `Successfully installed … bridge-balance-api-0.1.0 …`, exit 0, no conflict and no resolution warning. `pytest -q` → **`7 passed, 1 warning in 2.78s`**, exit 0. Only `tests/test_health.py` exists (there is no `test_chat.py`); `tests/__pycache__/` confirms the `**/__pycache__/` ignore entry was needed. **Unblocking change made without prior approval — flagged for CP0 ratification:** the line `"better-auth>=0.4.0",` was deleted from `pyproject.toml`. Evidence: `pip index versions better-auth` → `No matching distribution found`, every published release requires Python ≤3.11 while this project requires ≥3.12, and `grep -i better[_-]auth` across `edu-site/` shows **no module imports it** (only `app/config.py:82-83` settings fields and `.env.example:36-38` placeholders). It is reversible with one line; the constitution's `better-auth.com (Phase B)` intent is untouched and the dependency returns with the auth spec. **Sequencing note:** 0.4 was done before 0.3, so `.env` already held `LLM_PROVIDER=commandcode` when pytest first ran, which fails collection at `app/config.py:128` because the enum does not accept `commandcode` until §7.3 adds it. The baseline above was therefore taken with `LLM_PROVIDER=none` in the process environment (pydantic-settings gives env vars precedence over `.env`), which is the state 0.3's baseline is defined against. |
| 2026-09-23 | 0.4 | Command Code key saved to `edu-site/api/.env`; `LLM_PROVIDER=commandcode`. | `git check-ignore -v edu-site/api/.env` → `.gitignore:42:.env edu-site/api/.env`, exit 0; `git status --untracked-files=all` does **not** list the file. Created from `.env.example` with `LLM_PROVIDER=commandcode`, `COMMANDCODE_API_KEY` (present — 93 characters, prefix `user_`; the value was never printed and is not written anywhere in this file) and `COMMANDCODE_BASE_URL=https://api.commandcode.ai/provider/v1`. The key exists in exactly one place on disk, was written into no tracked file, PHR, doc or commit message, and was not echoed back to the owner. |
| 2026-09-23 | 0.5 | Provider probe written, run and independently verified. | `edu-site/api/scripts/probe_provider.py` created and kept (new, untracked until the CP0 commit); `ruff check` on it → `All checks passed!`. Run as `.venv\Scripts\python.exe scripts\probe_provider.py` from `edu-site/api`; the wrapper reported `EXITCODE=0`. All eight sections produced results — (d) deliberately records two HTTP 400s and (i) self-skipped because chat completions worked. Headline results: **(a)** `deepseek/deepseek-v4.1-flash` is listed, `supported_endpoints = ["/chat/completions", "/responses"]`; **(b)** non-streamed 200 with `usage`; **(c)** 61 chunks, `[DONE]` seen, one final chunk carries `usage`; **(d)** `tool_choice: "auto"` returns one `tool_call` and the tool-result round-trip returns the final answer, but **`tool_choice: "required"` → 400 `Thinking mode does not support this tool_choice`** (forced-function form too); streamed tool-call deltas are usable, so `buffer_streamed_tool_calls` is not needed; **(e)** `reasoning_content` is **absent** — the provider returns `reasoning` + `reasoning_details` and `usage…reasoning_tokens`; `reasoning_effort` low/high are accepted but their effect is unproven; **(f)** `x-cmd-zdr: 1` → **200**; **(g)** wrong key → 401, unknown model → 400, both with real error bodies; **(h)** the Agents SDK path works — 34 text deltas, the tool was called, `context_wrapper.usage` filled (`requests=2 input_tokens=823 output_tokens=211 total_tokens=1034`). Decisions settled: `LLM_API_STYLE = chat_completions`, `LLM_ZDR = true`, `LLM_REASONING_SUPPORTED = true`. Two findings that change §7.4: `tool_choice: "required"` is unavailable, so the modes' "search the book first" requirement must be carried by the instructions; and `reasoning_tokens` are charged against `max_tokens` (a 32-token call produced zero content characters), so `max_output_tokens` must leave room for reasoning above the visible answer. Results recorded in `specs/012-chatbot-tutor/research.md` §1. **Independent verification** (separate subagent, no shared context) confirmed the record against the captured output for a, d, e, f, g and h, confirmed `research.md` sections 2–4 are untouched, and confirmed secret containment: a repo-wide grep for the key's literal prefix, over tracked files, untracked files and `HEAD` history, returned no hits, and `.env` is still ignored (`.gitignore:42`). It found one unsupported claim, now fixed: §1 had cited a first effort run (`low 37 / high 13`) that appears in no captured artifact — corrected to the single captured run (35 vs 34). **Spend: ~$0.05** (the build prompt's own estimate; the first live provider spend of this build, inside the shared $1.00/day ceiling). |
| 2026-09-23 | 0.6 | Dependencies added and installed: backend `chat` extra, frontend markdown packages. | **Backend.** The `chat` extra added to `pyproject.toml`: `openai-agents~=0.22.3`, `llama-index-core~=0.14.25`, `llama-index-vector-stores-qdrant~=0.10.3`, `llama-index-embeddings-fastembed~=0.7.0`, `fastembed~=0.8.1`, `qdrant-client~=1.19.1`, `sqlalchemy[asyncio]>=2.0.30`, `asyncpg>=0.29.0`, `alembic>=1.13.0`, `tzdata~=2026.4`; the core `qdrant-client` floor was bumped `>=1.10.0` → `>=1.16.0` (the requirement the Qdrant vector store actually declares). **Every pin was verified against PyPI before use rather than taken from the build prompt:** `openai-agents` 0.22.3 exists and *is* the newest version, `requires_python >=3.10`, and it requires `starlette>=1.3.1` — the installed starlette 1.7.0 already satisfies that, so the prompt's conditional "if FastAPI conflicts, raise FastAPI's minimum" **did not trigger** and no version was raised. `pip install -e ".[dev,chat]"` exit 0 with no conflict and no resolution warning; installed: openai-agents 0.22.3, openai 3.19.1, llama-index-core 0.14.25, llama-index-vector-stores-qdrant 0.10.3, llama-index-embeddings-fastembed 0.7.0, fastembed 0.8.1, qdrant-client 1.19.1, SQLAlchemy 2.0.54, asyncpg 0.31.0, alembic 1.20.0, tzdata 2026.4, onnxruntime 1.30.0, with fastapi 0.141.1 and starlette 1.7.0 left untouched. `pip check` → **`No broken requirements found.`** and `pytest -q` still `7 passed`, exit 0. **Frontend.** `react-markdown@^10.1.0` and `remark-gfm@^4.0.1` added to `edu-site/package.json`, after verifying React 18 support from registry metadata first: `react-markdown@10.1.0` declares `peerDependencies.react: ">=18"`, and `remark-gfm@4.0.1` declares no `peerDependencies` and no `engines` at all and dedupes onto the copy Docusaurus 3.10.2 already resolves. `npm install` exit 0 with no peer conflict, `npm ls` shows both at the intended versions with no `UNMET`/`invalid`, React stayed 18.3.1, `npm run typecheck` exit 0. **Two risks recorded, not fixed:** (1) both packages are ESM-only (`"type": "module"`, no `require` entry in `exports`), so whether Docusaurus's webpack bundles `react-markdown` cleanly is **UNVERIFIED until a component imports it** — the real test lands at CP6; (2) `npm install` reported `49 vulnerabilities (1 low, 40 moderate, 8 high)` and `removed 38 packages` from `node_modules`; the lockfile diff is additive-only (40 insertions, 1 deletion — the deletion being a comma), so no locked package was dropped, but whether the vulnerability count is pre-existing is **UNVERIFIED** (no before/after audit was taken). |
