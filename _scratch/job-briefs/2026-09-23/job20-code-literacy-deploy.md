# Job 20: the public name "Code Literacy" everywhere, then deploy the site to Vercel
Effort: max. Live repository C:/Users/Dell/Desktop/Book only; never `.claude/worktrees/`; NO git commands ever, and never push to GitHub. Before editing a file, copy it byte-for-byte to `_scratch/backup-2026-09-23-code-literacy/` under the same relative path. Keep line endings and encoding; minimal edits in each file's own voice (plain, warm, easy English). Never hand-edit generated files. If a shell command times out, redirect its output to a log and poll it.

## Part A: the name (owner decision, 23 September 2026)
The skill that canon calls internally "Reading and Understanding Literacy" now has its public name: **Code Literacy**. At its first use on any page it is always introduced as "Code Literacy: reading and judging code"; after that, "Code Literacy" alone. Its meaning (`curriculum-state/canon/thesis.md` line 63): the ability to read code, whoever or whatever wrote it, and judge it well enough to verify it and own it. The owner deliberately keeps the word "literacy", because it carries understanding and evaluating (the OECD PISA definition of reading literacy: "understanding, using, evaluating, reflecting on and engaging with texts"). Record this as the owner's deliberate exception to `curriculum-state/canon/naming.md` Rule 1.
1. Replace every reader-visible use of the internal name, across `edu-site/docs/**` (not `edu-site/parked/**`), `edu-site/src/**`, `edu-site/docusaurus.config.ts`, `edu-site/static/**` and the root `README.md`. Known places: `edu-site/src/pages/index.tsx` (around line 34), `edu-site/src/components/DataViz/CurriculumDonut.tsx` (around line 66), `edu-site/src/components/DataViz/CurriculumBarChart.tsx` (around line 72). Changelog entries dated before 2026-09-23 stay as they are. Where a reader-facing page describes this skill without a name and the name would help, add it; keep every such change to one short phrase.
2. Glossary: add an entry for Code Literacy in `edu-site/docs/glossary.md`, in that file's own format (plain words, one or two sentences). Where a docs page names it at first use, link it the site's usual way, `[*Code Literacy*](/glossary#<slug>)`.
3. Canon, each in the file's own format: `naming.md` (the public name, the first-use rule, and the owner's exception to Rule 1 with the reason above); `thesis.md` (next to the internal name: public name Code Literacy, decided 23 Sep 2026; do not rename the internal name); `course-structure.md` if it keeps a decision log (the next CS id, DECIDED, 23 Sep 2026).
4. `edu-site/docs/changelog.md`, the 2026-09-23 entry, in its own grouping: one line saying the book's core skill now has its public name, Code Literacy: reading and judging code.
5. In `edu-site/`: `npm run build` passes, `npm run typecheck` passes, `npm run lint` no more than 64 errors and 3 warnings, `node scripts/check-chapter.mjs` shows no errors.

## Part B: deploy to Vercel (the owner asked for the changes to be live)
1. Read-only first: find how the live site is deployed. Look for `.vercel/` folders (repo root and `edu-site/`) and `vercel.json`; check whether the Vercel CLI is installed (`vercel --version`, or `npx vercel --version`) and logged in (`vercel whoami`); which project is linked; its root directory and build settings; the current production domain.
2. If the CLI is logged in and linked to that project: deploy the current working copy to production from the linked directory (`vercel --prod --yes`), wait for it to finish, and record the deployment URL and the production domain. If the remote build fails, read its log (`vercel inspect <deployment-url> --logs`); fix only a small configuration mismatch the log names (for example the Node version) and retry once; otherwise stop and report the log's error lines.
3. If the CLI is not logged in, not linked, or the only route would be a git push: change nothing, and write down the exact commands the owner must run (for example `vercel login`, then `vercel link`, then `vercel --prod`), from which folder. Never type or store any credential.
4. After a deploy, fetch the production homepage and confirm it contains "Zero background needed" and "Code Literacy", and does not contain "Harvard".

## Part C: records
1. Prompt record under the next free id in `history/prompts/general/` (check right before writing; model on `0146-*`): title "Code Literacy name and Vercel deploy", date 2026-09-23, feature none, labels ["naming","code-literacy","deploy"]. Prompt text: the LAST user message in `C:/Users/Dell/.claude/projects/C--Users-Dell-Desktop-Book--claude-worktrees-book-content-pipeline-mindmap-26fb57/6a922ad9-5682-4e67-a94e-e18925c8952a.jsonl` that contains "must be used", extracted exactly by script. Response text: "Named the core skill Code Literacy (reading and judging code), rewrote the script's first part with it, and had DeepSeek apply the name everywhere and deploy the site to Vercel."
2. Agenda `_scratch/job-briefs/2026-09-22/agenda/index.html`: copy to `agenda/index.pre-job20.html` first; content edits only, under the Ground rules of `_scratch/job-briefs/2026-09-22/job8b-agenda-refresh.md`. Header time and tally. "Waiting for you": remove the question about the reading skill's public name. "Done today", at the top: "Code Literacy named everywhere" (one line: files changed, build result) and, if Part B deployed, "Site live on Vercel" (the production domain and the time); remove the chore "Deploy the site…". If Part B stopped, put the exact commands into that chore instead.

## Report (at most 25 lines)
Files changed, grouped; the four gate results; how the site is deployed; the deploy result, with the URL, or the exact commands for the owner; the live-site check; Agenda tags inside `<main>` balanced and everything outside `<main>` byte-identical to `index.pre-job20.html`.
