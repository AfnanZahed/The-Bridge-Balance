# Job 17b: four small site fixes before the intro recording
Effort: max. Live repository C:/Users/Dell/Desktop/Book only; never `.claude/worktrees/`; no git. Before editing a file, copy it byte-for-byte to `_scratch/backup-2026-09-23-demo-fixes/` under the same relative path. Keep line endings and encoding; minimal edits in each file's own voice and code style. Background: items 2 to 5 of section 1 in `_scratch/job-briefs/2026-09-23/job16-demo-plan.md`. The owner approved them on 23 September 2026. Another job (17a) is reading the repo at the same time; it writes nothing you touch.
1. Homepage buttons: both hero buttons and the closing "Begin Stage 1" button point at the Welcome page (its real route). Adjust a label only if it would now be false.
2. Homepage lines: fix "Built for working developers" (the reader starts as a complete beginner and finishes as an expert: `curriculum-state/canon/audience.md`), "Est. Hours 48h" (the book commits to no total: remove it, or replace it with a true fact the layout can hold), and "Every chapter ships twice: a video lecture…" (true today: the written material comes first, a video lecture follows each part). Do NOT change the hero heading: the owner will zoom on it on camera.
3. The Stage 1 and Stage 2 opening pages (`edu-site/docs/stage-01-*/index*` and `stage-02-*/index*`, whatever the real files are): remove the old-placeholder wording and every "Stage 3" mention; say in two or three plain, warm sentences what the stage will teach (from `curriculum-state/canon/course-structure.md`) and that its chapters are being written now. Frontmatter stays valid.
4. Search: the generated index must list only pages that exist and are not `placeholder` (searching "Binary" today returns a result that 404s). Fix the generator script, not the generated file.
5. One entry dated 2026-09-23 in `edu-site/docs/changelog.md`, in its current format and grouping, describing what a reader will notice.
6. In `edu-site/`: `npm run build` passes, `npm run typecheck` passes, `npm run lint` no more than 64 errors and 3 warnings. Then search the built index for "Binary" and confirm no dead result.
7. Prompt record under the next free id in `history/prompts/general/` (check right before writing; model on `0143-*`), title "Demo fixes and intro script", date 2026-09-23, feature none, labels ["demo","homepage","marketing"]. Prompt text: the owner's LAST user message, extracted exactly by script from `C:/Users/Dell/.claude/projects/C--Users-Dell-Desktop-Book--claude-worktrees-book-content-pipeline-mindmap-26fb57/6a922ad9-5682-4e67-a94e-e18925c8952a.jsonl` (it contains "build 2 to 5"). Response text: "DeepSeek built demo fixes 2 to 5; Claude wrote the voice-over script and the multi-shot plan, and checked Composio for social posting."
8. Agenda `_scratch/job-briefs/2026-09-22/agenda/index.html`: copy to `agenda/index.pre-job17.html` first; content edits only, under the Ground rules of `_scratch/job-briefs/2026-09-22/job8b-agenda-refresh.md`. Header time and tally. Remove the "Waiting for you" question about which site basics to build. "Done today", at the top: "Site fixed for the intro recording" (one line with what changed and the build result). "Small chores for you": add "Deploy the site, so the live address shows the three stages" and "Make the 1200×630 share image" if not already there.
Report (at most 20 lines): each change in one line; the three command results; the "Binary" check; what the sidebar shows under Stage 0, 1 and 2 when expanded (labels and badges); Agenda tags inside `<main>` balanced and everything outside `<main>` byte-identical to `index.pre-job17.html`.
