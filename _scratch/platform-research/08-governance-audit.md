## C. GOVERNANCE

### C1. stack.md (`stack.md`, "Source of truth. Last updated 2026-08-17", `stack.md:3`)

**Phased rollout table IN FULL** (`stack.md:83–88`):

| Phase | Goal | Stack additions |
|---|---|---|
| **A — Curriculum live** | Docusaurus + RAG chat working on real chapters | nothing new; what we have today |
| **B — First sale** | One product purchasable end-to-end | + Stripe Checkout (hosted page) + Neon tables for `products`, `orders` + a single Cloudflare R2 bucket + Resend for the receipt email |
| **C — Catalog of 5+** | Catalog page, multiple products, license keys, versioned downloads | + R2 signed URLs + license-key generator (DIY) + Inngest for async email |
| **D — Polish** | Refunds, customer portal, version updates, reviews | + Stripe Customer Portal + Stripe Tax + Resend drip campaigns |

**Do-not-add list IN FULL** (`stack.md:94` "Temptations explicitly rejected during planning:", `stack.md:96–104`): ~~Next.js storefront~~ — Docusaurus covers the storefront as React components; ~~Supabase~~ — overlaps Neon + better-auth + R2; ~~Tailwind + shadcn~~ — Docusaurus uses Infima; fighting the framework loses; ~~Prisma~~ — wrong ecosystem; use SQLAlchemy; ~~Bun / Deno runtime~~ — Node is fine for a docs site; ~~Vercel Functions / Cloudflare Workers for backend~~ — breaks the moment you need >10s jobs; ~~Custom admin panel~~ — `psql` + Drizzle Studio, that's it; ~~tRPC / GraphQL~~ — REST + OpenAPI is enough; ~~Third-party license-key service~~ — buy only if Phase C needs it.

**Lines on the named surfaces**
- LLM + embeddings: `stack.md:15` "| 7 | LLM + embeddings | **OpenAI Python SDK** | Chat, translation, personalization, `text-embedding-3-small` |"; `stack.md:26` (diagram) "OpenAI | $5 credit for new accounts | real usage" (`:70`); risk `stack.md:113` "**OpenAI dependency.** Have a fallback (Ollama / DeepSeek / Anthropic) before you reach 1k paying customers."
- Embeddings: only `stack.md:15` (`text-embedding-3-small`). Nothing else in stack.md.
- Vector DB: `stack.md:14` "| 6 | Vector search | **Qdrant Cloud** (Free Tier) | RAG over MDX chapters |"; diagram `stack.md:50–51`; free-tier row `stack.md:69` "| Qdrant Cloud | 1 GB vectors, free forever | >1 GB |".
- Chatbot: `stack.md:36` (diagram) "│  - Chatbot widget   │"; `stack.md:11` "| 3 | Backend API | **FastAPI** (Python 3.12) | RAG chat, product API, checkout, webhooks, license endpoints |"; `stack.md:85` (Phase A goal, quoted above).
- Where FastAPI runs in production: `stack.md:42` "│  FastAPI container  │  Render / Railway / Fly.io"; free tier `stack.md:67` "| FastAPI on Render | 750 hrs/mo free | >1 always-on instance |". No single host chosen.
- Database: `stack.md:13` "| 5 | Database | **Neon Serverless Postgres** | Users, products, orders, licenses, translation cache |"; `stack.md:68` "| Neon | 0.5 GB Postgres | 2nd branch or >0.5 GB |".
- Auth: `stack.md:16` "| 8 | Auth | **better-auth.com** | Sign-up, sign-in, profile collection (hardware/software background) |"; `stack.md:75` "| better-auth | free (self-hosted) | never |".
- Payments: `stack.md:17` "| 9 | Payments | **Stripe** | Checkout, subscriptions, customer portal, webhooks |"; `stack.md:71` "| Stripe | pay-as-you-go, no monthly fee | per-transaction % |"; `stack.md:111` "Phase B sells without license keys (signed download URL is enough)."
- Cost: `stack.md:25` "All have free tiers sufficient for the first 1k paying customers."; `stack.md:77` "**Realistic Phase A cost at <100 paying customers: $0–$5/month.** Mostly OpenAI tokens."
- **Rate limits: NOT FOUND in stack.md.** Nearest: `history/adr/0001-free-tier-llm-choice.md:20` (Gemini "15 RPM / 1M TPM / 1500 RPD") and `curriculum-state/canon/course-structure.md` (provider table, secrets row).

### C2. Constitution (`.specify/memory/constitution.md`)

**Version** `:562` "**Version**: 3.1.0 | **Ratified**: 2026-08-17 | **Last Amended**: 2026-09-22"; v3.1.0 amended 2026-09-22 on the Agentic-inside-SE decision (`:2–11`); v3.0.2 amended 2026-09-21 (`:50`); v3.0.1 amended 2026-09-20 (`:90`); v3.0.0 ratified by the owner 2026-09-20 (`:140`).

Operative sentences:
- **I. Free-Tier by Default** `:399` — "Every component of the platform MUST run on a free tier until measurable usage justifies a paid tier. The decision to leave a free tier requires evidence — paying-customer count, traffic, or a documented operational need — recorded in an ADR."
- **II. Pluggable Provider Abstraction** `:407` — "Every external service MUST be accessed through a thin interface owned by this codebase. Provider SDKs are imported lazily inside the implementation module — the app never requires an SDK it isn't actively using." Also `:410` "**OpenAI is gated.** `LLM_PROVIDER=openai` is supported but explicitly last-resort."
- **VI. Free, Open Curriculum** `:455` — "The textbook is **100% free** for students. There is no paywall on content, no premium chapter, no email-gated download."
- **VII. Apple-Design Purity** `:462` — "The platform's visual identity models **Apple's design language** at the level the free tier allows: calm, uncluttered surfaces; generous negative space; floating layered panels with soft elevation; glass-layer translucency; a restrained monochrome-neutral palette with one focused accent per surface; and a seamless light/dark experience."
- **VIII. Research-and-Comparison Discipline** (NON-NEGOTIABLE) `:477` — "Nothing is authored from memory or instinct — no lesson, chapter, heading, platform feature, setting, or button — before two research passes complete, for the content track and the platform track alike"; blocking second pass `:480` "This is a **blocking input** — drafting MUST NOT begin until it is supplied"; platform scope `:492` "A new page, feature, setting, or button undergoes the same two passes".
- **IX. Beginner-First Content Integrity** `:501` — "**Stages 0 and 1 are written for absolute beginners and for nobody else** … **There is one reader in every stage: a beginner when they start, an expert when they finish** … **No rule, draft or review may assume a separate senior reader is present.**"

Secrets/testing/observability/perf/accessibility: **Secrets** `:528` "never committed. `.env` is gitignored. `.env.example` documents every variable and its phase." ; **Config** `:527` "all runtime config MUST flow through `app/config.py` (Pydantic Settings). No environment reads in business code. No hardcoded URLs, model names, or thresholds."; **Logging** `:529` structured, "Free-form `print()` calls MUST be replaced before Phase B ships."; **Errors** `:530`; **Health** `:531` "Phase A: only LLM credential presence. Phase B: adds DB + Qdrant."; **Testing** `:549` "the textbook MUST build (`npm run build`) and the backend MUST pass `pytest -q` before any phase ships."; **Design fidelity/a11y** `:533` (blur fallback, light/dark tokens, `prefers-reduced-motion`, focus-visible, no hard borders); **Auth boundary** `:539` "better-auth.com (Phase B). Routes that mutate user state MUST verify the session."; **Phase discipline** `:523`; supply chain `:541`. **Performance budgets: NOT FOUND in the constitution** — the only perf-budget requirement is `CLAUDE.md:305` ("Performance: p95 latency, throughput, resource caps") inside the architect-guidelines block.

### C3. ADRs (`history/adr/`)

| # | Title | Status | Date |
|---|---|---|---|
| 0001 | Free-Tier LLM Choice (No OpenAI Lock-in) | Accepted | 2026-08-17 |
| 0002 | Text-First, Video-Second Workflow (Principle III Amendment) | Accepted | 2026-08-17 |
| 0003 | Scoped Accent Reintroduction | Accepted | 2026-09-03 |
| 0004 | Two-Skill Content Split | Accepted · amended 2026-09-07 · superseded in part by 0007 | 2026-09-06 |
| 0005 | Search Experience Split | Accepted | 2026-09-11 |
| 0006 | Beginner-First Content Integrity — Constitution v3.0.0 | Accepted | 2026-09-20 |
| 0007 | `lesson-spine-authoring` Becomes Bridge Balance-Specific | Accepted · migration closed 2026-09-22 | 2026-09-20 |
| 0008 | The Chapter Component Palette Is Open | Accepted · corrected 2026-09-20 | 2026-09-20 |
| 0009 | Owner Input Gates | Accepted | 2026-09-20 |
| 0010 | Agentic AI Taught Inside Software Engineering: the Three-Stage Structure | Accepted | 2026-09-22 |

Constraining decisions/consequences:
- **ADR-0001** — Decision `:17` "**No OpenAI by default.** The shipped default is `LLM_PROVIDER=none`, which boots the backend but refuses every completion with HTTP 503"; `:18` pluggable abstraction "All chat / translation / personalization calls go through `app.llm.LLMClient.generate(...)`"; `:19` preference order Ollama → Groq → Gemini → Together → DeepSeek → OpenAI "last resort"; `:21` "**Embeddings** follow the same pattern. Default is `EMBEDDING_PROVIDER=none`. First preference … **HuggingFace sentence-transformers** (`all-MiniLM-L6-v2`)"; `:24` "**Phase A**: the abstraction ships, but no provider is wired … the LLM feature is deferred to Phase B." Consequences `:29–30` "The default `none` provider cannot spend money… Switching providers is a config change"; negative `:37` "The LLMClient interface stays narrow on purpose."
- **ADR-0005** — Decision `:26` "**The site has exactly two search surfaces, one engine, and one result-card language — all client-side, all free.**"; consequences `:37` "**Client-side search has a ceiling.** … O(n) scan per keystroke is imperceptible today, but it will not survive a corpus an order of magnitude larger."; Alternatives `:78` "**Add a search service — Algolia DocSearch or a Qdrant-backed endpoint.** Rejected: … Qdrant + embeddings is Phase B work (this repo is Phase A, routers return 501)".
- **ADR-0002** — `:35` "the eventual RAG chatbot (Phase B) — well-structured MDX is good retrieval data"; `:42` "Until Phase B wires the chatbot, a viewer of the video has to navigate to the chapter manually."
- **ADR-0008** — `:126` "`Quiz` and `Progress` need persistence, which is Phase B/C in `stack.md`; they cannot be in an early batch without opening that phase."
- **ADR-0009** — gates that bind the chat build too: comparative study blocks "Drafting anything, content or platform"; model choice per task (`:38–45`).
- ADR-0003/0004/0006/0007/0010 do not touch LLM providers, the chatbot, RAG, Qdrant, embeddings, backend or hosting.

### C4. Phase B status

- `_scratch/platform-research/00-CONSOLIDATED-RESEARCH-AND-ROUGH-PLAN.md:4` — "**Owner decision this session:** Phases B and C are open, spec-first."
- `_scratch/platform-research/01-chatbot-brief-for-planning.md:112` — "Phase B is now open, so it is being wired up".
- `history/prompts/general/0141-open-phases-launch-pass1-research.general.prompt.md:26` (owner) — "Hi there, I am going to do 3 big things in the website, 1. auth, 2. 100% real multi agent Rag chatbot, 3. payment system."; `:33` "**Phase gate** → 'Open B and C, spec-first.'"; `:36` "**Research gate** (Principle VIII) → 'DeepSeek does pass 1, my study follows.'"
- Contradiction still live: `CLAUDE.md:97` "**Phase A only, today.** … don't wire them in ahead of that phase being explicitly opened"; `.claude/skills/bridge-balance-project-guide/SKILL.md:100` same.
- **NOT FOUND in `curriculum-state/`** (only Stage-0 "Phase A/Phase B" content-era usage, a different meaning — `curriculum-state/proposals/whole-book-redesign-record-2026-09-14.md:488–494`).

### C5. Integrity floor (`curriculum-state/canon/integrity-floor.md`)

Operative rule `:14–16`: "> **When a chapter touches an external credential, licence, examination, or platform, that body's own published policy outranks this project's curriculum documents. Where they conflict, STOP and surface the conflict to the user. Never author through it.**" Applies to features: `:24` "The same rule covers: platform terms of service, professional licensing bodies, academic institutions, certification providers, and any published code of conduct a reader could be sanctioned under." For an AI feature: `:55–61` compliant framing — AI for understanding; "CS50's own Duck for in-course help, named explicitly as the permitted tool."; `:61` "**Never** AI producing work that gets submitted." `CLAUDE.md:57` restates: "A task that would have a chapter, a feature, or a prompt coach a reader through graded CS50 work stops and surfaces the conflict."

### C6. Voice / audience / corrections

`curriculum-state/canon/voice.md`, 12 most operative lines: `:27` "**Write as a warm, expert teacher talking to one student they like.**"; `:30–31` "**context-aware, humanised, emotionally intelligent.**"; `:44–49` the two personas "they never mix" — content-track teacher vs `CLAUDE.md` engineering persona; "Check which track you are on before writing the first sentence."; `:83` "**4. Label what kind of claim you are making.**"; `:89` "**5. Say the hard part.**"; `:94` "**6. Be exact backstage. Be plain on the page.**"; `:99–101` "**Research is how the writer becomes correct. It is not a list of things the reader must be told.**"; `:108–110` "**Every word must make sense to this book's actual reader.** … This test outranks specificity, elegance and concision, every time."; `:125–127` "**The anti-commodity test — per chapter** … Every chapter makes at least one **signature contribution**."; `:142–146` "**Do not cut words that carry warmth**"; `:165` phrases "banned outright, not discouraged"; `:219` "**The register follows the reader, not the file.**"; `:251–254` a name "carries a why, not only a what … supplied per topic — if it is missing, stop and ask."

`audience.md:24` reader definition IN FULL: "**One reader, in every stage: a beginner when they start, an expert when they finish.**"

`corrections.md`:
- **§19** `:391` "The owner picks the model for every piece of content, every time" — `:406–408` "Before any content task starts, **ask which model does which part** — planning, drafting, research, revision — and wait for the answer. There is no default."
- **§21** `:441` "The chapter component palette is open, and it is being widened" — `:482–484` "**The palette is open and expected to grow.** Tabs, collapsible sections, step cards, try-it panels, key-takeaway boxes, comparison cards, timelines — and whatever else serves the reader."; `:485–486` "**What stays closed is a drawing delivered as inline markup.**"
- **§29** `:716` "Examples are Pakistani-first" — `:724` "**The first choice is a concrete, everyday example from life in Pakistan:** a bank token number, a load-shedding schedule, an Easypaisa or JazzCash transfer, a cricket score, a rickshaw fare, prices in rupees."
- **§30** `:737` "'Ask which model, every time' includes standards research" — `:743` "Before any standards research starts, ask the owner which model does it, and wait for the answer. DeepSeek stays the usual choice for research, but it is never assumed."
- **Entries mentioning a chatbot/assistant/AI feature: NOT FOUND** (no entry names a chatbot or assistant). Entries governing UI labels/buttons: **§3** `:91` "Before writing any rejection into a rule file, decide which layer it belongs to: the content, or the design."; **§9** `:211` "**Internal names need a reader-facing name, decided deliberately.** … **ask the owner first** rather than shipping the internal label."; **§13** `:261` "**Audience labels are allowed.**"; **§15** `:285` owner: "also for the website's whole thing, like the buttons, or any UI, UX thing or any marketing part of the site"; `:295` "**This covers the platform too:** pages, flows, buttons, labels, settings, marketing copy."; **§21** `:501` "**The Source chip** … is part of this work".

### C7. Research-and-comparison for platform/UI work

`curriculum-state/canon/research-and-comparison.md:3` "**No claim in this project is made from memory or guesswork.** Every lesson, every chapter, every heading, every feature, every option, every button is grounded in studied external practice and then deliberately taken further."; `:70` "## This applies to the platform too" — ":71 **A new page, a new feature, a new setting, a new button, a new label** gets the same two passes"; `:74` "adopt their reasoning, their patterns and their structure freely — what stays ours is the visual language (Constitution Principle VII) and the words."

**Owner Pass 2 for the chatbot: NOT FOUND anywhere.** `_scratch/`, `reference-material/` (no chatbot hits, only a vendored lesson HTML file), `design-reference/` (no hits), `curriculum-state/proposals/` (only `welcome-page-brief-2026-09-14.md:53` "no RAG chatbot"), `history/` — nothing but: `01-chatbot-brief-for-planning.md:145–151` "**The owner's own hands-on comparison (research pass 2).** The owner is going to use real products hands-on and hand over their notes. Until that lands, the spec is provisional … Their shortlist is NotebookLM, Claude/ChatGPT with the chapters uploaded, Perplexity, Khanmigo, and a dev-docs assistant (Mintlify, ReadMe's Ask AI, Fern's Ask Fern, or Kapa.ai)."; and `00-CONSOLIDATED-RESEARCH-AND-ROUGH-PLAN.md:245–249` calling the supplied Gemini pass "thin in a specific way … not the owner's own comparative study."

## D. SPECS AND PRIOR PLANNING

### D1. `specs/`

Folders: `001-book-foundation/` (spec, plan, tasks, research, data-model, quickstart, README, contracts/content-schema.md, checklists/requirements.md); `001-hamburger-rebuild/` (spec, plan, tasks, research, data-model, quickstart, contracts/useDrawer.md, checklists/requirements.md); `002-apple-design/` (+ contracts/design-system.md); `003-remove-llm-providers-page/` (+ contracts/documentation-boundary.md); `004-fix-navbar-alignment/` (+ contracts/visual-contract.md, verification/ 10 PNGs); `005-redesign-navbar-hero/` (+ contracts/presentation-contract.md, verification/ 12 PNGs); `006-storytelling-intro/` (+ 4 contracts, 8 verification files); `007-search-experience/` (spec, plan, tasks); `008-faq-page/` (spec only); `009-code-of-conduct/` (spec only); `010-accessibility-statement/` (spec only); `011-curriculum-redesign/` (spec, plan, tasks); plus `specs/README.md` (index stops at 007, `specs/README.md:25`).

**No chat/chatbot/assistant/tutor spec exists: NOT FOUND.** Next free feature number: **012** (highest used 011; 001 is duplicated).

### D2. `.specify` and commands

`.specify/templates/`: adr-template.md, agent-file-template.md, checklist-template.md, phr-template.prompt.md, plan-template.md, spec-template.md, tasks-template.md. `.specify/scripts/powershell/`: check-prerequisites.ps1, common.ps1, create-new-feature.ps1, setup-plan.ps1, update-agent-context.ps1. **sp command files: `.claude/commands/` holds all four plus eight more** — `sp.phr.md`, `sp.specify.md`, `sp.plan.md`, `sp.tasks.md`, `sp.adr.md`, `sp.analyze.md`, `sp.checklist.md`, `sp.clarify.md`, `sp.constitution.md`, `sp.implement.md`, `sp.reverse-engineer.md`, `sp.taskstoissues.md`. None in `.specify/` (only memory/scripts/templates) or `.claude/skills/`; stale copies exist under `.claude/worktrees/*/.claude/commands/`.

### D3. `_scratch/platform-research/01-chatbot-brief-for-planning.md` — complete digest

**Framing** (L1–10): chatbot brief, written 22 Sep 2026 (`:3`); "decisions are settled; no code and no spec exist yet" (`:4`); purpose "hand this to a planning agent to produce the chatbot's architecture and plan (spec → plan → tasks, per this repo's SDD flow). Execution then returns to the original agent." (`:5–7`); "not a plan, a spec, or a wish list. It is the set of decisions already made, so the planner does not relitigate them." (`:8–9`); "Read first: `CLAUDE.md`, `stack.md`, `.specify/memory/constitution.md`." (`:10`).

**§1 What is being built** (`:14–24`): (1) "A grounded question-answering chatbot for the book at `thebridgebalance.app`. It answers student questions, primarily from the book's own chapters, and shows where each answer came from." (`:16–17`); (2) "**This is the first of three features.** Auth and payments are decided in principle and are out of scope for this planning pass — **do not design them here**, but do not design anything that forbids them either" (`:19–21`); (3) "**Not in scope for this pass:** login, the free-question gate, the paywall, saving chat history, exams, progress tracking. All four arrive later." (`:23–24`).

**§2 Locked decisions** (`:30–40`): L1 "**Not multi-agent.** Six modes implemented as settings/variants on one agent." — owner "was shown that this option is explicitly *not* multi-agent, and chose it anyway. Upgradeable later; must not require a rewrite." (`:32`); L2 "**Search/store: Qdrant.** Already named in `stack.md`. Free tier: 1GB RAM, 4GB disk. Open source, so no lock-in." (`:33`); L3 "**Model: DeepSeek v4.1 Flash, reached through the Command Code API key.** Command Code exposes an **OpenAI-compatible** endpoint, so a Python backend can call it normally. Verified, not assumed." (`:34`); L4 "**Build the brain first; the gate later.** The grounded answering service is built and tested with no login in front of it." (`:35`); L5 "**The student picks the mode per question**, and the mode decides whether the answer draws on the book or on general knowledge. So the six modes are the product surface, not decoration." (`:36`); L6 "**Every answer drawn from the book must show its source.** This is already promised to readers in the existing preview UI and must be honoured." (`:37`); L7 "**Eventually: login required, 10–20 free questions, only the cheap modes free.** Deep Research and Deep Think are Pro-only, because they cost many times more per question. **Not built in this pass** — but the design must make it addable." (`:38`); L8 "**Chat history is saved for paying users only.** Arrives after payments." (`:39`); L9 "**Pro sells features, never a chapter.** Constitution Principle VI: the textbook is free forever, no paywall on content." (`:40`).

**§3 Assumptions** (`:46–54`): A1 "**Index everything published:** the chapters, plus the reference pages (glossary, FAQ, welcome). **Exclude `edu-site/parked/`** entirely — the credentials track is unpublished." (`:46–47`); A2 "**Re-indexing is a script you re-run** when chapters change. No automatic/CI pipeline yet." (`:48`); A3 "**The free allowance resets monthly.**" (`:49`); A4 "**The corpus is small today and will grow by orders of magnitude.** 36,002 words across 31 files right now … **Design for the target, ship something that works at today's size.**" (`:50–53`); A5 "**No custom admin panel**" (`:54`).

**§4 Open for the planner** (`:60–76`), 10 items: chunking strategy/size/overlap/headings-as-context (`:62`); "Embedding model and provider — note `text-embedding-3-small` is what `stack.md` currently names, but ADR-0001 gates OpenAI as *last resort* and prefers free tiers, so this is a genuine conflict to resolve rather than assume." (`:63–65`); dense-only vs hybrid vs reranking (`:66`); ingestion shape — where the offline ingest script lives/how run/how index rebuilt (`:67–68`); "The retrieval seam — one interface in front of search, so Qdrant can be swapped or the corpus can outgrow a single context window without a rewrite." (`:69–70`); how the six modes are expressed: prompt variants, parameters, or tools (`:71`); prompt design and how "show me your source" is represented in the API response (`:72`); wiring `edu-site/src/components/ChatAssistant/` "without breaking its current honesty guarantees" (`:73–74`); "Rate limiting and abuse control at the API boundary." (`:75`); "Evaluation: what minimal check proves answers are grounded and not invented." (`:76`).

**§5 Constraints** (`:82–122`): LLM access via existing abstraction `edu-site/api/app/llm/` (`base.py`, `registry.py`, `router.py`, `providers/*.py`), via `get_llm_client()`; "**Extend this; do not replace it.**" (`:83–85`); a new provider touches five files, listed in `edu-site/api/README.md` (`:86`); "`app/main.py` already wires a `chat` router. **Phase B routers currently raise `HTTPException(501)`** with their Pydantic models already defined. Fill in the handler body in place — do not restructure the router or its models." (`:87–89`); config via `app/config.py`, no hardcoded model names/URLs/thresholds (`:90–91`); secrets never in code, `.env.example` documents every variable (`:92`); named typed errors mapped at the router boundary (`:93`); "**Free tier is mandatory** (Principle I). Every component needs a documented free tier and a 'when you would start paying' line." (`:96–97`); provider abstraction/no lock-in (Principle II, `:98`); do-not-add list normative, reproduced (`:99–101`); Apple-Design Purity for anything visible, "tokens already live in `edu-site/src/css/` — match them, do not invent a new visual language." (`:102–103`); "**Smallest viable change** (Principle V). No drive-by refactors." (`:104`); "**Principle VIII (NON-NEGOTIABLE):** nothing is designed before two research passes. Pass 1 … is done … **Pass 2 — the owner's own hands-on comparison — is still outstanding** (§7). The plan should say plainly what it is waiting on." (`:105–107`); frontend contract: the component "is a finished design with **deliberately no API call anywhere**, and a comment saying there must not be one until the phase that owns it opens. Phase B is now open, so it is being wired up — but note what it currently promises readers: six modes with one-line descriptions; 'Ask the book anything'; answers that show where they came from; and an honest reveal that says nothing is transmitted while it is not switched on. **The honesty behaviour must not become a lie** — if something is unimplemented, it must still say so." (`:110–115`); quality gates: frontend "`npm run build` (a five-gate chain: `check:frontmatter` → `check:refs` → `check:chapter` → chapter manifest → search index → `docusaurus build`), plus `npm run typecheck` and `npm run lint`."; backend "`pytest -q`, `ruff check .`, `mypy app`."; "Both must be green before anything is considered done." (`:118–122`).

